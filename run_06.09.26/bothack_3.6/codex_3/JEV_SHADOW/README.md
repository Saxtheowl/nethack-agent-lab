# JEV_SHADOW

BotHack plays unchanged.  Jev observes interesting decisions and its choices
are written with `executed: false` to `jev_decisions.jsonl`.

Run: `python3 JEV_SHADOW/run.py --out runs/shadow-42 --seed 42 --goal minetown`.

