# nhlight's NetHack configuration.
# Provenance: adapted from BotHack's upstream/bothack.nethackrc (TAEB-derived
# settings) in the historical workspace; the glyph remapping is kept because
# it is a game-display fact the screen parser depends on, not bot logic.

ROLE=valkyrie
OPTIONS=race:dwarf

OPTIONS=!bones
OPTIONS=lit_corridor

# more feedback!
OPTIONS=msg_wall_hits

OPTIONS=!sparkle
OPTIONS=runmode:teleport
OPTIONS=!timed_delay

# display
OPTIONS=!showexp
OPTIONS=showscore
OPTIONS=time
OPTIONS=color
OPTIONS=boulder:8
OPTIONS=!tombstone
OPTIONS=!news
OPTIONS=!legacy
OPTIONS=suppress_alert:3.4.3
OPTIONS=hilite_pet

# functionality
OPTIONS=!autopickup
OPTIONS=pickup_types:$
OPTIONS=pickup_burden:unburdened
OPTIONS=!prayconfirm
OPTIONS=pettype:none
OPTIONS=!cmdassist
OPTIONS=disclose:yi +avgc
OPTIONS=menustyle:partial

# miscellaneous
OPTIONS=!mail

# map changes for code simplicity
OPTIONS=monsters:abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ@X'&;:~m
# "strange monster" = m (mimic)
# ghost/shade = X     now space is solid rock

OPTIONS=traps:\^\^\^\^\^\^\^\^\^\^\^\^\^\^\^\^\^\^\^\^\^
# spider web = ^      now " is amulet

OPTIONS=dungeon: |--------||.-|]]}}.##<><>_\\\\{{~.}..}} #}
#  sink => {          one less #, walkable
#  drawbridge => }    one less #, not walkable
#  iron bars => }     not walkable (color: cyan)
#  trees => }         not walkable (color: green)
#  closed doors => ]  now + is spellbook
#  grave => \         now gray | and - are walls

OPTIONS=objects:m
# "strange object" = m (mimic)
